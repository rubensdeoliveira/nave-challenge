import styled from 'styled-components'

export const Container = styled.button`
  display: flex;
  align-items: center;

  border: 0;
  background: transparent;
  position: absolute;
  top: 21px;
  right: 21px;
`
